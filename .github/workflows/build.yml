name: Kernel Build

on:
  push:
    branches: [ "main"]
  pull_request:
    branches: [ "main"]

jobs:
  build-kernel:
    name: Build
    runs-on: ubuntu-latest
    
    container:
      image: ghcr.io/tabower/aker-rs-env:latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Compile Kernel
        run: make